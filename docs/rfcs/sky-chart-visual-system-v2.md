---
id: RFC-SKY-CHART-VISUAL-SYSTEM-V2
type: request-for-comments
status: APPROVED
related:
  - REVIEW-SKY-CHART-DIRECTION-2026-09-23
  - PLAN-SKY-CHART-HOME-REDESIGN-V2
  - DESIGN-VISUAL
  - DESIGN-IX-A11Y
  - ADR-ADAPTIVE-IMMERSIVE-HOMEPAGE
  - ADR-SKY-CHART-HOMEPAGE-RUNTIME
  - RFC-ADAPTIVE-IMMERSIVE-HOMEPAGE-PRODUCTION-V1
  - PAGE-HOME
  - PROJECT-EVIDENCE
  - GOV-ENGINEERING-LIFECYCLE
last_verified: 2026-09-24
---

# Sky Chart visual system, v2 — Home and App Bar redesign

## Context

[`GOV-ENGINEERING-LIFECYCLE`](../governance/engineering-lifecycle.md) classes a major UI/styling system and design-system architecture as a consequential change that requires a Governance PR before implementation. The current Home and global App Bar are governed by the approved [`RFC-ADAPTIVE-IMMERSIVE-HOMEPAGE-PRODUCTION-V1`](adaptive-immersive-homepage-production-v1.md), its [`ADR-ADAPTIVE-IMMERSIVE-HOMEPAGE`](../decisions/adaptive-immersive-homepage.md), and the applicable sections of [`DESIGN-VISUAL`](../design/visual-language.md) and [`DESIGN-IX-A11Y`](../design/interaction-responsive-accessibility.md).

On 2026-09-23 the repository owner reviewed three prototyped directions — A Sky Chart, B Line Map, C Deployable Sheet — recorded in [`REVIEW-SKY-CHART-DIRECTION-2026-09-23`](../reviews/sky-chart-direction-2026-09-23/index.md), and chose Direction A · Sky Chart, with C · Deployable Sheet's personalization technique applied to specific sections, and a preference for semi-transparent cards wherever they stay legible and original. The owner also confirmed that implementation opens with this Governance PR, followed by a superseding ADR where runtime changes are recorded, and authorized use of Figma before implementation.

[`PLAN-SKY-CHART-HOME-REDESIGN-V2`](../plans/active/sky-chart-home-redesign-v2.md) carries the resolved direction into a twelve task/PR execution plan. That plan's `status` stays `PROPOSED` and no implementation task may start until this RFC receives the owner's gate G1 approval and Task 2 records the superseding ADR and approved design sections. This RFC implements nothing; it is the governance step the plan and the lifecycle require.

## Proposal

The resolved world is a navigator's star atlas, detailed in [`PLAN-SKY-CHART-HOME-REDESIGN-V2`](../plans/active/sky-chart-home-redesign-v2.md) section 2 ("Approved design direction") and normatively specified in that plan's section 6 design decisions D-01 through D-27. This RFC references those sections by link rather than restating them, so the plan stays the single normative source of the numeric specification.

In summary:

- A full-viewport celestial sphere sits behind Home's content through the hero and the four chapters. Its named stars are FURLANICH's approved process vocabulary, revealed and joined into constellations as the visitor scrolls, then receding to a dim ground after the chapters.
- Two translucent materials carry content: an **atlas plate** (dark, registration corner ticks, plate number) for chapters, Services, Proof, Process and Founder; and a **plotting sheet** (translucent bone vellum, plotting grid, bearing label, corner crease) for Problems and the new illustrative Position fix section. Problems uses a stepped three-sheet cascade with a "cocked hat" glyph, translating Direction C's vellum gesture into navigation vocabulary.
- The final CTA band moves from night to Bone, the brand's canonical ground.
- The global App Bar becomes a floating "chart header" in atlas-plate material on every route; on Home it starts transparent and prints the current section as a mono readout.

The normative visual reference is the self-contained prototype at `docs/reviews/sky-chart-direction-2026-09-23/reference/sky-chart-reference.html`, together with the plan's numeric specification. Where the reference prototype and the plan's section 6 differ, the plan wins.

Implementation follows the plan's task/PR packets 1–12 across five execution waves (plan section 17), each independently reviewable and revertible, gated by the plan's canonical validation command and Playwright/accessibility/performance suites (plan sections 14 and 15).

## Supersession list

Approval of this RFC proposes to supersede the following eighteen approved-record boundaries. Each item states the boundary it retains, but the list as a whole is not a claim that nothing outside these eighteen items changes: the plan's D-rules (section 6) are the complete normative specification, and this list is this RFC's best-effort account of the approved rules they touch. Where this RFC is not certain a sub-list (for example, of affected pages or records) is complete, it says so rather than asserting completeness. The independent review of this RFC's first draft found the original nine-item list incomplete — most materially, it omitted the removal of the derived Azure sculpture (item 14) — and the plan (section 21, Task 1 packet, `PLAN-SKY-CHART-HOME-REDESIGN-V2` at `5e92916`) was extended to these eighteen items and this no-completeness-claim rule as a result.

1. **`VISUAL-IDENTITY-V1` "UI gradients, neon and glass remain excluded."** Quoted exactly from [`DESIGN-VISUAL` › "### Color roles"](../design/visual-language.md#color-roles): "Bone and azure are the recognizable pair. Full-azure surfaces are reserved for deliberate emphasis. UI gradients, neon and glass remain excluded." Retained boundary: superseded for Home and the global App Bar only. Home introduces the two translucent chart materials (atlas plate D-05, plotting sheet D-06) and their `backdrop-filter` treatment, plus these UI gradients: the D-02 ground radial gradient, the D-03 scrim linear gradients, the D-06 plotting-sheet grid and corner-crease gradients, and the D-19 dawn-CTA gradient. Neon remains excluded everywhere, including Home and the App Bar.
2. **Light-only foundation / no dark theme.** Quoted exactly from [`DESIGN-VISUAL` › "Homepage foundation visual baseline — APPROVED" › "### Direction"](../design/visual-language.md#direction): "The first implementation is light-only. A dark theme is not required for implementation or release of the homepage foundation." Also [`VISUAL-IDENTITY-V1` › "### Precision Assembly composition"](../design/visual-language.md#precision-assembly-composition): "No dark theme, portrait, evidence upgrade or new icon system is approved by this revision." Retained boundary: superseded for the Home environment only (the fixed celestial-sphere ground and scrim). It is not a site theme: Services, Projects, Studio, Founder, Contact and Privacy content stays on the approved light foundation, and no theme switcher is introduced. The App Bar's dark chart-header material is a separate supersession, covered by items 1 and 4, and applies on every route, not only Home; only the page *content* of non-Home routes stays light-only. The site-wide [`DESIGN-VISUAL` OPEN item](../design/visual-language.md#open-target-decisions-beyond-the-commercial-homepage-services-page-projects-experience-studio-founder-and-contact) "Whether a dark theme is ever introduced." stays OPEN; this RFC resolves nothing site-wide.
3. **`IMMERSIVE-HOME-V1.1` C2 composition.** Owned by [`DESIGN-VISUAL` › "IMMERSIVE-HOME-V1.1 — C2 Adaptive System Instrument — APPROVED"](../design/visual-language.md#immersive-home-v11-c2-adaptive-system-instrument-approved) and [`DESIGN-IX-A11Y` › "IMMERSIVE-HOME-V1.1 — Adaptive hybrid interaction — APPROVED"](../design/interaction-responsive-accessibility.md#immersive-home-v11-adaptive-hybrid-interaction-approved), approved by [`RFC-ADAPTIVE-IMMERSIVE-HOMEPAGE-PRODUCTION-V1`](adaptive-immersive-homepage-production-v1.md) and recorded in [`ADR-ADAPTIVE-IMMERSIVE-HOMEPAGE`](../decisions/adaptive-immersive-homepage.md) — the two-zone editorial-anchor/instrument-viewport composition, the four static chapter posters and the phase spine. Retained boundary: the four chapters, their approved heading IDs and copy stay; the framed instrument viewport, the four poster images and `PhaseSpine` are replaced by the full-viewport environment and atlas-plate chapters (plan D-01, D-12, section 19 file matrix). The ADR's retained runtime boundaries (gates, budgets, direct Three.js, the Framer Motion progress boundary, demand rendering, one-shot initialization, session context-loss handling) are not superseded by this RFC; see "Runtime changes" below.
4. **`DESIGN-VISUAL` and `DESIGN-IX-A11Y` Global app bar Surface/Border treatment.** `DESIGN-VISUAL` [› "### Global app bar — APPROVED"](../design/visual-language.md#global-app-bar-approved): "The shared header is a sticky app bar with `top: 0`, Surface background, and a `1px` Border separator. It remains in normal document flow and does not hide, translate, or animate based on scroll direction." Also [› "#### Semantic roles in production"](../design/visual-language.md#semantic-roles-in-production), `surface` row: "the approved Surface role for the app bar, footer and compact panel." `DESIGN-IX-A11Y` [› "## Global app-bar behavior — APPROVED"](../design/interaction-responsive-accessibility.md#global-app-bar-behavior-approved): "uses the approved Surface and Border treatment, and does not hide or translate on scroll." Retained boundary: the Surface/Border fill and colors are superseded on every route by the atlas-plate chart-header material (plan D-22); `top: 0` sticky positioning and normal document flow are retained unchanged. The Home dock transition — background and border easing between transparent and the atlas-plate fill, keyed to `scrollY` — is the only scroll-linked change D-22 introduces. Hiding, translating or animating the bar based on scroll *direction* stays prohibited everywhere, including Home. D-22 also moves `BrandSignature` from the azure-on-bone mark at 40px (see item 14) to the approved bone-on-azure variant, a 32px Azure tile with the Bone mark at 22px; the mark's protected geometry is unchanged.
5. **`DESIGN-VISUAL` "Do not force a viewport-height hero."** (Homepage foundation visual baseline, Spacing and grid). Retained boundary: superseded for Home only, where the hero uses `min-height: 100svh` under the App Bar (plan D-11). No other page adopts a viewport-height hero, and Home's content growth is never clipped even where it exceeds the viewport.
6. **`DESIGN-VISUAL` Home Canvas/Surface section alternation** ([› "### Section rhythm and surfaces"](../design/visual-language.md#section-rhythm-and-surfaces): "Alternate only the approved Canvas and Surface backgrounds to clarify the long page: Problems uses Surface; Services uses Canvas; Audiences uses Surface; Proof uses Canvas; Process uses Surface; Founder uses Canvas; and the final CTA uses Action tint."). Retained boundary: superseded for Home, where the alternating Canvas/Surface backgrounds are replaced by transparent sections over the fixed environment, carried by atlas plates and plotting sheets (plan D-01, D-13 to D-19). The final CTA's Action-tint band is itself superseded by D-19's dawn band — see item 15 below; this item does not claim that role is preserved.
7. **`DESIGN-VISUAL` Home card radius and shadow rules** ([› "### Shared surface patterns"](../design/visual-language.md#shared-surface-patterns): "Surface background, `1px` Border, `16px` radius, no shadow"). Retained boundary: superseded for Home's card-equivalent surfaces, which adopt the atlas plate (18px radius, layered inset/drop shadow, D-05) and plotting sheet (6px radius, drop shadow, D-06) treatments instead. The rule is unaffected outside Home; it still governs the Projects evidence cards ([`VISUAL-IDENTITY-V1` › "#### Static editorial application (Services and Projects)"](../design/visual-language.md#static-editorial-application-services-and-projects), "Evidence cards": "Surface, 1px rule, 16px radius, content-driven height") and the Services index panel ([› "### Service index"](../design/visual-language.md#service-index): "a compact, labelled Surface navigation panel with a `1px` Border, `16px` radius").
8. **`DESIGN-VISUAL` exclusion of "floating telemetry, particle fields"** (`IMMERSIVE-HOME-V1.1` C2 composition scene description: "Generic dashboard cards, floating telemetry, particle fields, neon, continuous rotation, elastic motion and ornamental glass are excluded."). Retained boundary: superseded to the extent that the new environment renders named, textual process-vocabulary stars (plan Appendix B) joined by constellation link segments, and up to 520 dim, unlabeled field stars (plan T-07) as atmospheric texture. Continuous rotation, elastic motion and ornamental glass remain excluded; the scene is demand-rendered from scroll position only (plan T-06).
9. **The new `HOME-IMPACT` section and its bilingual strings.** An addition, not a supersession: a new "Position fix" section (`impact`) carrying the honest illustrative business-impact visualization specified in plan section 12 and the new bilingual strings in plan Appendix A. It adds no invented evidence, percentage, duration or currency figure. Its insertion into `PAGE-HOME`'s approved section order is covered by item 18 below.
10. **`DESIGN-VISUAL` "Do not introduce gradients, glass effects, decorative shadows, or additional accent colors for these sections."** ([› "### Section rhythm and surfaces"](../design/visual-language.md#section-rhythm-and-surfaces)). Retained boundary: superseded for Home sections, which use the atlas-plate and plotting-sheet materials and the D-02, D-03, D-06 and D-19 gradients named in item 1; no accent hue outside the D-04 `sky.*`/`chart.*` blues is added.
11. **`DESIGN-VISUAL` Home section rhythm and type scale** ([› "### Typography"](../design/visual-language.md#typography): "Hero H1: `64/68px` wide, `48/52px` medium, and `36/40px` compact."; [› "### Section rhythm and surfaces"](../design/visual-language.md#section-rhythm-and-surfaces): "Standard section padding is `64px` compact, `80px` medium, and `96px` wide."; [› "### Later-section typography"](../design/visual-language.md#later-section-typography): "Section H2: `32/38px` compact and `40/48px` medium/wide..."). Retained boundary: superseded on Home only, where D-09 (type scale) and D-10 (spacing) replace them; every other page keeps the existing rhythm and type scale.
12. **`DESIGN-VISUAL` and `DESIGN-IX-A11Y` Home grid rules.** `DESIGN-VISUAL` [› "### Shared surface patterns"](../design/visual-language.md#shared-surface-patterns): "Cards in each repeated homepage group use equal, content-driven grid tracks based on the tallest card in that group"; and [› "### Section-specific presentation"](../design/visual-language.md#section-specific-presentation) per-section column counts (Problems "one column compact, two columns medium and wide"; Services "one column through medium widths and three columns wide"; Process "one column compact, two columns medium, and four columns wide"). `DESIGN-IX-A11Y` [› "### Reflow and responsive behavior"](../design/interaction-responsive-accessibility.md#reflow-and-responsive-behavior) states matching per-section column counts ("Problems and Audiences use one column compact and two columns from medium upward"; "Services uses one column at compact and medium widths and three columns at wide widths"; "Process uses one column compact, two columns medium, and four columns wide") and "Homepage Problems, Services, and Audiences cards use equal content-driven grid tracks within each repeated group and grow together from the tallest card." Retained boundary: superseded on Home only, where D-13 (Problems cascade), D-14 (Services catalogue plates) and D-17 (Process ecliptic) replace the equal-track card grid and column counts; non-Home grids are unaffected.
13. **`VISUAL-IDENTITY-V1` "Cards remain reserved for genuine comparison, bounded evidence and controls; ordinary ideas use editorial composition rather than a bordered rectangle."** ([› "### Precision Assembly composition"](../design/visual-language.md#precision-assembly-composition)). Retained boundary: superseded on Home, where chapters, Proof, Process and Founder use atlas plates as ordinary editorial composition rather than bounded-evidence cards; Problems and Position fix use plotting sheets for the same reason. The rule is unaffected outside Home.
14. **The derived Azure sculpture as the scene's identity element.** Owned jointly by `VISUAL-IDENTITY-V1` ([› "### Identity assets"](../design/visual-language.md#identity-assets): "The immersive homepage uses a separate derived sculpture that may fragment and reconnect."; [› "### Precision Assembly composition"](../design/visual-language.md#precision-assembly-composition): the four visual beats, from "a resolved three-layer sculpture establishes the identity beside the proposition and actions" to "the sculpture resolves as one system"), `IMMERSIVE-HOME-V1.1` ([› "### Chapter compositions"](../design/visual-language.md#chapter-compositions): "Recognition: bone editorial surface, intact derived sculpture, proposition and primary action."; [› "#### WebGL enhancement"](../design/visual-language.md#webgl-enhancement): "The scene reuses the poster vocabulary in three dimensions. It has three extruded Azure chevron slabs lit by one ambient and one directional light on Bone, plus small Ink channel tiles and thin Azure route lines."), and [`ADR-ADAPTIVE-IMMERSIVE-HOMEPAGE` › "## Decision"](../decisions/adaptive-immersive-homepage.md#decision): "The canonical Contained Master remains a protected DOM/SVG asset. Only the separate derived sculpture enters the scene." **Retained boundary and explicit ask:** the star-atlas scene has no sculpture, no chevron slabs and no Ink channel tiles; its identity element is the named-star environment itself. The protected Contained Master mark's geometry is unchanged and it stays out of the scene, but item 4's App Bar supersession moves where it appears: `DESIGN-VISUAL` [› "#### Identity asset files"](../design/visual-language.md#identity-asset-files) records the current App Bar mark as "the azure-on-bone mark at 40 px beside the live 16 px `FURLANICH` wordmark"; D-22 replaces it with the approved bone-on-azure variant, a 32px Azure tile (radius 7px) holding the Bone mark at 22px, beside a Bone 16px wordmark. This mark change belongs to item 4, not to this item's scene-removal ask. Because removing the sculpture drops an approved identity element rather than only restyling it, **this RFC explicitly asks the owner to accept the removal of the derived Azure sculpture** as a distinct part of the gate G1 decision, not folded into the other supersessions.
15. **The Action-tint final CTA band on Home** (`DESIGN-VISUAL` [› "### Section-specific presentation"](../design/visual-language.md#section-specific-presentation), "Final CTA": "Use a full-width Action-tint band with a narrow text measure, the standard primary action, and the standard secondary action."). Retained boundary: on Home, D-19's dawn band — a gradient from transparent to Bone — replaces the Action-tint final CTA. Every other page keeps its existing Action-tint ending unchanged (`VISUAL-IDENTITY-V1` "Endings": Services, Projects and project details keep the Action-tint inquiry band).
16. **The Pause control's placement and the `IMMERSIVE-HOME-V1.1` responsive choreography table** (`DESIGN-IX-A11Y` [› "### Scroll and motion control"](../design/interaction-responsive-accessibility.md#scroll-and-motion-control): "A visible Pause motion control beside instrument status pauses WebGL updates and active video without changing the chapter."; and [› "### Responsive choreography"](../design/interaction-responsive-accessibility.md#responsive-choreography) five-width table). Retained boundary: superseded on Home. Instrument status itself is not removed — D-25 keeps `Plate {current} of 04` inside the Pause pill at ≥768px — but two things change: the control moves from the stage's lower edge to a fixed viewport corner (`right: 16px; bottom: calc(16px + env(safe-area-inset-bottom))`), shown only while the chapter span is in view rather than mounted with the sticky stage; and the phase readout is hidden below 768px instead of showing beside a duplicated status. The plan's section 10 scene mapping and D-11–D-19 layout rules replace the five-width choreography table. The control's keyboard operability, `aria-pressed` state and supplementary role beside `prefers-reduced-motion` are retained.
17. **The ADR's permission for one optional Connection film.** Stated in [`ADR-ADAPTIVE-IMMERSIVE-HOMEPAGE` › "## Decision"](../decisions/adaptive-immersive-homepage.md#decision): "One optional authored video may appear inside the Connection instrument window."; also in `DESIGN-VISUAL` [› "IMMERSIVE-HOME-V1.1" › "### Instrument media"](../design/visual-language.md#instrument-media): "One authored video is permitted only in Connection when it materially improves the explanation."; and `DESIGN-IX-A11Y` [› "IMMERSIVE-HOME-V1.1" › "### Progressive structure and media lifecycle"](../design/interaction-responsive-accessibility.md#progressive-structure-and-media-lifecycle): "Direct Three.js and one optional native Connection video are progressive enhancements." **Retained boundary:** this permission is **WITHDRAWN, pending the owner's gate G1 decision** (plan section 30, updated after this RFC's second review round; plan section 5 non-goals: "The Connection film (still omitted), video, audio or generated imagery."); the sky-chart runtime carries no video surface. Every other ADR budget is retained unchanged, including the main-thread interaction task gate (`DESIGN-IX-A11Y` "Production gates" and the ADR's gate table: "Main-thread interaction task | <50 ms"), restated in plan section 14.
18. **`PAGE-HOME`'s approved section order and narrative.** The current approved order is recorded in [`PAGE-HOME` › "## MKT-D05-HOME — Consolidated narrative — APPROVED revision 1"](../product/pages/home.md#mkt-d05-home-consolidated-narrative-approved-revision-1) (2026-09-16): "Accepted source order under D05: global demo notice → HOME-HERO → HOME-PROBLEMS (with audience context) → HOME-SERVICES → HOME-PROOF → HOME-PROCESS → HOME-FOUNDER → HOME-CTA. Retire only the standalone HOME-AUDIENCES presentation; preserve its relevant audience meaning in Problems and Services." `CommercialHomepage.tsx` renders exactly this order. Retained boundary: `HOME-IMPACT` (`impact`) is inserted between Services and Proof (plan D-15; Task 9 packet: "problems, services, impact, proof, process..., founder, cta"); every other approved section and its narrative purpose stays. **Pre-existing inconsistency, outside this PR's scope:** `PAGE-HOME`'s earlier "## Section order" list (lines 35–46) still names a standalone `HOME-AUDIENCES` entry that MKT-D05-HOME already retired on 2026-09-16; that list is stale relative to the approved D05 narrative and the shipped component. This RFC does not correct it — Task 2 updates `PAGE-HOME`'s section order as part of recording the `impact` insertion, and can reconcile the stale list at the same time.

## Runtime changes

This RFC proposes no runtime or dependency change; it authorizes only the design direction and this supersession list. The plan (section 8, "Architecture impact") records that Task 2, after this RFC's approval, creates a new ADR (working id `ADR-SKY-CHART-HOMEPAGE-RUNTIME`) superseding [`ADR-ADAPTIVE-IMMERSIVE-HOMEPAGE`](../decisions/adaptive-immersive-homepage.md). That superseding ADR:

- **Keeps** the current ADR's gates (reduced motion, Save-Data, WebGL2, session context-loss flag, near-viewport, `load`), all production budgets — including the retained main-thread interaction task gate (<50 ms) — direct Three.js (no React Three Fiber), the existing Framer Motion progress boundary, demand-driven rendering with no idle loop, one-shot initialization, and session-scoped context-loss handling.
- **Adds** the full-viewport fixed canvas portaled to `document.body`, `THREE.CanvasTexture` sprite text labels, a recede/suspend behavior once the visitor scrolls past the chapters, and a locale-neutral static poster pair (`environment-wide.webp`, `environment-compact.webp`) in place of the four chapter posters.
- **Removes** the derived Azure chevron sculpture and its Ink channel-tile labels from the scene (supersession item 14 above, which the RFC asks the owner to accept explicitly), and withdraws the ADR's permission for one optional Connection film (supersession item 17 above); the runtime carries no video surface.

The production budgets in [`ADR-ADAPTIVE-IMMERSIVE-HOMEPAGE`](../decisions/adaptive-immersive-homepage.md) and [`RFC-ADAPTIVE-IMMERSIVE-HOMEPAGE-PRODUCTION-V1`](adaptive-immersive-homepage-production-v1.md) are otherwise unchanged by this RFC; the plan's section 14 performance strategy restates them as the gate for the sky-chart runtime (≤120 KiB Brotli incremental JavaScript, DPR caps, the <50 ms main-thread interaction task, scroll-frame and Core Web Vitals budgets). No new npm dependency is proposed (plan T-01); `package.json` and `package-lock.json` stay locked.

The superseding ADR is out of scope for this Governance PR and is not created here. It is Task 2's deliverable, gated on this RFC's approval (gate G1).

## New copy

The only new bilingual copy this direction requires is listed in [`PLAN-SKY-CHART-HOME-REDESIGN-V2`](../plans/active/sky-chart-home-redesign-v2.md) Appendix A ("New bilingual strings"), referenced here rather than duplicated. It covers: the decorative instrument coordinate line, plate-label and status-label templates; the four chapter kickers; the Services, Proof and Process section kickers; the App Bar readout labels; the Proof accountability log; and the complete `HOME-IMPACT` (Position fix) content — heading, introduction, illustrative-scenario tag, toggle labels, figure titles and descriptions, the five source/note pairs, and the impact-counts labels and caption. Every other Home and App Bar string is existing approved content; no approved copy is changed by this RFC.

## Alternatives

The full Stage 1 prototype comparison (screenshots, skill log and measured contrast for all three directions) was published separately to the owner; only its resolution is recorded in the repository, in [`REVIEW-SKY-CHART-DIRECTION-2026-09-23`](../reviews/sky-chart-direction-2026-09-23/index.md#owner-disposition-2026-09-23). This RFC records that resolution honestly rather than reconstructing evidence that is not in the repository.

- **Direction B · Line Map.** Prototyped alongside A and C. **Verdict: not chosen.** The owner's recorded disposition names Direction A as the selection and does not carry any Line Map element into the resolved world; no Line Map material or personalization technique appears in the plan's design decisions.
- **Direction C · Deployable Sheet.** Prototyped alongside A and B, built around a vellum surface and a stepped-cascade gesture. **Verdict: not chosen as the primary direction, but its personalization technique is adopted inside Direction A.** The owner's recorded disposition: "Direction A · Sky Chart, with more personalization on sections like 'When work is spread across tools', where a style similar to C · Deployable Sheet is better. Semi-transparent cards are always preferable as long as the result is astonishing and original."
- **The owner's hybrid choice.** Direction A · Sky Chart is the resolved environment and App Bar direction. Direction C's vellum plotting-sheet material and stepped-cascade gesture are translated into navigation vocabulary and applied specifically to Problems (three cascading plotting sheets, each with a "cocked hat" glyph) and to the new Position fix section, per plan D-06 and D-13. No other section adopts Direction C material; Services, Proof, Process and Founder stay in the Direction A atlas-plate material (plan D-05, D-14, D-16 to D-18).

## Risks

- **Real-device thermal behavior** of a full-viewport canvas combined with `backdrop-filter` surfaces on low-end Android is unproven; the plan mitigates with DPR caps, star-count limits, a blur budget of at most three intersecting `backdrop-filter` surfaces per viewport (App Bar excluded), and recede-triggered render suspension (plan section 30, D-08, D-24).
- **Contrast and legibility on translucent surfaces.** The direction review already found that Azure fails as text on the dark ground (1.99:1) and that Mist falls to 3.86:1 on translucent surfaces in the worst composite; the plan's D-04 token table and `scripts/design-tokens.test.mjs` are the mitigation, asserting every pairing including worst-case composites.
- **Scope creep beyond Home and the App Bar.** The plan's non-goals (section 5) and file/ownership matrix (section 19) bound every task to Home and the global App Bar; Services, Projects, Studio, Founder, Contact and Privacy content is explicitly excluded from restyling.
- **Governance drift between the Figma mirror and this record.** The Figma mirror at Gate F (below) can fall out of sync with an approved deviation; the plan treats Figma as non-normative and requires either a batched Figma update or a recorded drift note in the acceptance review (plan section 30).
- **Spanish content expansion** (approximately 30% longer strings) can break the new plotting-sheet and atlas-plate layouts; the plan requires both-locale testing at every width and forbids shrinking type below the D-09 minimums as a fix (plan section 30).
- **Budget regression.** If the sky-chart runtime exceeds the unchanged 120 KiB Brotli ceiling, the plan requires stopping and returning to governance rather than raising the budget inside an implementation PR (plan section 28).

The Figma mirror for this direction (Gate F) exists at `https://www.figma.com/design/V6FD6Sq3gqqxeMw5Si7Dnx` and is non-normative: if it disagrees with the plan, the plan wins, and implementation never waits on it.

## Decision requested

The repository owner's gate G1 approval is requested for:

1. The Sky Chart · Direction A environment, atlas-plate and plotting-sheet material system, and floating chart-header App Bar described above and normatively specified in [`PLAN-SKY-CHART-HOME-REDESIGN-V2`](../plans/active/sky-chart-home-redesign-v2.md) section 6 (D-01 to D-27).
2. The eighteen supersessions listed above, each bounded as stated — **including, as a distinct decision, item 14's explicit removal of the derived Azure sculpture** as the scene's identity element.
3. The runtime-change summary above, authorizing Task 2 to draft the superseding `ADR-SKY-CHART-HOMEPAGE-RUNTIME` for separate review; this RFC does not itself accept that ADR.
4. The new copy referenced from plan Appendix A.

Approval unlocks Task 2 (decision closure: the superseding ADR, the approved `DESIGN-VISUAL`/`DESIGN-IX-A11Y`/`PAGE-HOME` sections, and this plan's status change to `APPROVED`) and, after Task 2 merges, Wave 1 of the implementation plan. Approval does not itself authorize any implementation PR, dependency change, or release step. Rejection or requested changes keep the plan `PROPOSED` and keep every approved record above unchanged; no code task starts.

## Status

**PROPOSED — awaiting the owner's gate G1 decision.** Record the outcome here and link the resulting ADR (`ADR-SKY-CHART-HOMEPAGE-RUNTIME`, created by Task 2) once approved.

**APPROVED — 2026-09-24.** The repository owner approved and merged this RFC as Governance PR #77 (merge commit `70168e9`), accepting the Sky Chart · Direction A environment, atlas-plate and plotting-sheet material system, floating chart-header App Bar, all eighteen supersessions listed above (including item 14's explicit removal of the derived Azure sculpture), the runtime-change summary, and the new copy referenced from plan Appendix A. Task 2 / PR 2 records the accepted decision in [`ADR-SKY-CHART-HOMEPAGE-RUNTIME`](../decisions/sky-chart-homepage-runtime.md) and the approved `DESIGN-VISUAL`/`DESIGN-IX-A11Y`/`PAGE-HOME` sections, and sets `PLAN-SKY-CHART-HOME-REDESIGN-V2`'s status to `APPROVED`. This approval does not itself authorize any implementation PR, dependency change, or release step.
